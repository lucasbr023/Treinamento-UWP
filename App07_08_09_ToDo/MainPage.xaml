<Page
    x:Class="App07_08_09_ToDo.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App07_08_09_ToDo"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <local:Person x:Key="Person" />
        <DataTemplate x:Key="templateListView" x:DataType="local:Person">
            <TextBlock Text="{x:Bind Path=FullName}"/> 
        </DataTemplate>
    </Page.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition  Width="*"/>
            <ColumnDefinition  Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="25*"/>
        </Grid.RowDefinitions>
        
        <TextBox Grid.Column="0" HorizontalAlignment="Stretch" Text="{x:Bind Name, Mode=TwoWay}" VerticalAlignment="Top"/>
        <Button Grid.Column="1" VerticalAlignment="Top" Click="AddPersonToCollection">Add</Button>
        <ListView Grid.Row="1" Grid.ColumnSpan="2" ItemsSource="{x:Bind People, Mode=OneWay}" ItemTemplate="{StaticResource templateListView}"/>
    </Grid>
</Page>
